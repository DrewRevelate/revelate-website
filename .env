# Database connection (this will be replaced by Heroku's DATABASE_URL in production)
DATABASE_URL=postgres://localhost:5432/revelate

# Server settings
PORT=3000

# For local development only - DO NOT commit sensitive values to version control