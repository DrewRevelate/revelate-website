/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a placeholder configuration to help Vercel understand this is a static site
  // This file exists only for compatibility purposes
  reactStrictMode: true,
  output: 'export', // Static HTML export
};

module.exports = nextConfig;
