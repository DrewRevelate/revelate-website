<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <!-- Google Fonts - Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Main stylesheet -->
    <link rel="stylesheet" href="/assets/css/main.css">
    
    <% if (typeof metaDescription !== 'undefined') { %>
    <meta name="description" content="<%= metaDescription %>">
    <% } else { %>
    <meta name="description" content="Revelate Operations provides data-driven SaaS consulting services, helping businesses optimize their CRM, enhance data analytics, and develop revenue operations strategies.">
    <% } %>
    
    <% if (typeof metaKeywords !== 'undefined') { %>
    <meta name="keywords" content="<%= metaKeywords %>">
    <% } else { %>
    <meta name="keywords" content="SaaS consulting, data analytics, business intelligence, CRM management, revenue operations">
    <% } %>
</head>
<body>
    <!-- Navigation -->
    <%- include('../partials/navigation') %>

    <!-- Page Content -->
    <%- body %>

    <!-- Footer -->
    <%- include('../partials/footer') %>

    <!-- Scroll to Top Button -->
    <button id="scroll-top" class="scroll-top-btn" aria-label="Scroll to top">
        <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Login Modal -->
    <%- include('../partials/login-modal') %>

    <!-- JavaScript -->
    <script src="/assets/js/navigation.js"></script>
    <script src="/assets/js/main.js"></script>
    
    <% if (typeof pageScript !== 'undefined') { %>
    <script src="/assets/js/<%= pageScript %>.js"></script>
    <% } %>
</body>
</html>
